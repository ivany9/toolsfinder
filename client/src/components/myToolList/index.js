import React from 'react';
import { useMutation } from '@apollo/client';
// import myTooList from '../../components/myToolList'


const myToolList = () => {
  console.log("jdjdj")
  return (
    <div>
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
      The component to render user's tools
    </div>
  );
};

export default myToolList;
